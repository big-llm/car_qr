{
  "name": "car-qr-monolith",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix functions",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "npm start --prefix functions",
    "dev": "npm run dev --prefix functions"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "firebase-admin": "^12.0.0",
    "helmet": "^7.1.0",
    "joi": "^17.11.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.0",
    "@types/express": "^4.17.21"
  }
}
